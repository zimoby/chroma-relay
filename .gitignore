node_modules
.DS_Store
dist
dist-ssr
*.local
dist
yarn-error.log
.media
.env.production
# Runtime and visual evidence is local-only and never published.
/evidence/
# The formal build identity is generated from frozen output bytes and is never committed.
/dist/cep/.chroma-relay-build-provenance.json
